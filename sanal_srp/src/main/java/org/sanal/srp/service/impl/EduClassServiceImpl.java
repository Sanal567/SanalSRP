package org.sanal.srp.service.impl;

import org.sanal.srp.service.EduClassService;
import org.springframework.stereotype.Service;

//@Data
@Service
public class EduClassServiceImpl implements EduClassService {
/*
	@Autowired
	private EduClassRepository classRepository;

	@Override
	public List<EduClass> getAllClasses() {
		// List<EduClass> classes = classRepository.findAll();
		return (List<EduClass>) classRepository.findAll();
	}
*/
	/*
	 * @Autowired private EduClassDao eduClassDao;
	 * 
	 * public EduClassDao getEduClassDao() { return eduClassDao; }
	 * 
	 * public void setEduClassDao(EduClassDao eduClassDao) { this.eduClassDao =
	 * eduClassDao; }
	 * 
	 * @Override public List<EduClassVO> getAllClasses() {
	 * 
	 * List<EduClass> classes=eduClassDao.getAllClasses(); List<EduClassVO>
	 * classVOs=new ArrayList<>(); EduClassVO eduClassVO=null; for(EduClass
	 * eduClass:classes) { eduClassVO=new EduClassVO();
	 * BeanUtils.copyProperties(eduClass, eduClassVO); classVOs.add(eduClassVO); }
	 * return classVOs; }
	 */

}